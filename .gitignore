# Node / Vite
node_modules/
dist/
.vite/

# Tauri / Rust
src-tauri/target/
src-tauri/gen/
# Ollama sidecar binary (76 MB, no longer bundled — re-fetched on
# demand by scripts/fetch-ollama.sh if anyone re-enables that backend).
src-tauri/binaries/

# Per-PDF artifacts (never commit user data)
*.pdf
*.pdf.annot.json
.*.pdf.clips/
.pdf-annotator/

# OS / editor / Claude
.DS_Store
.claude/
.vscode/
.idea/
*.swp

# Env
.env
.env.local

# Landing build artifacts (regenerated by scripts/build-landing.mjs)
landing/download/
landing/schema/
landing/spec/index.html
landing/spec/spec.css
landing/spec.md
landing/_headers
landing/_redirects
